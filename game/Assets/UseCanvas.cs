using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UseCanvas : MonoBehaviour
{
    public bool redraw = false;
    // Start is called before the first frame update
}
